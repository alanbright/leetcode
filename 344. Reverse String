var reverseString = function(s) {
   return s.split('').reverse().join('') 
};

/*
var reverseString = function(s) {
    let reverse = '';
    for(let i = s.length -1; i >= 0; i --) reverse+=s[i];
    return reverse;
};
*/
