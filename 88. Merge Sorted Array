var merge = function(nums1, m, nums2, n) {
    for(i=0;i<n;i++){
        nums1[m+i] = Number(nums2[i]);
    }
    return nums1.sort(function(a, b){return a-b})
    // https://www.w3schools.com/jsref/jsref_sort.asp
};

/*
var merge = function(nums1, m, nums2, n) {
    nums1.length=m;
    nums2.length=n;
    nums1.push(...nums2);
    nums1.sort((a,b)=>a-b);
};
*/
