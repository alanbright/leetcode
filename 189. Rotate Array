var rotate = function(nums, k) {
    while(k > 0){
        nums.unshift(nums.pop())
        k--;
    }
    return nums
};

/*
var rotate = function(nums, k) {
    nums.splice(0, 0, ...nums.splice(nums.length - k))
};



var rotate = function(nums, k) {
    for(let i=0; i<k; i++){
      let num = nums.pop()
      nums.unshift(num)
    }
    return nums;
};

*/
