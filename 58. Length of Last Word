/**
 * @param {string} s
 * @return {number}
 */
var lengthOfLastWord = function(s) {
    //console.log(typeof s)
    if(s.length <= 0 || typeof s != 'string') return 0;
    console.log(s.trim().split(' ').pop())
    console.log(String(s.trim().split(' ').pop()).length)
    return s.trim().split(' ').pop().length
};
